FROM node:16

WORKDIR /usr/src/app

COPY package*.json ./
RUN npm install

COPY . .

ARG DEMO_FRONTEND_PORT
ENV DEMO_FRONTEND_PORT=${DEMO_FRONTEND_PORT}

EXPOSE ${DEMO_FRONTEND_PORT}

CMD ["npm", "start"]